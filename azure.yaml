# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: markup-teams
metadata:
  template: markup-teams@0.0.1-beta
services:
  web:
    project: .
    dist: build
    language: js
    host: staticwebapp
